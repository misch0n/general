# Repo map (auto-generated — `bash scripts/genmap.sh`; do not hand-edit)

Per-file index of section banners (`// ===== / // -----`) and top-level `function`s,
with line numbers. Grep/jump here instead of reading whole files. See CLAUDE.md for architecture.

## `features/core/boot.js`
```
  (no banners/functions)
```

## `features/core/core.js`
```
33    fn expUpperComplete()
34    fn vstarExp()
61  § ---- analytics (GoatCounter custom events; cookieless, no personal data) ----
67    fn _gcSend()
68    fn track()
82    fn trackGame()
320    fn renderChangelog()
328    fn toggleChangelog()
358    fn fun()
359    fn namePointsOn()
360    fn total()
363    fn gExp()
366    fn gManual()
368    fn setDockUI()
373    fn measureBarFull()
382    fn syncBottomPad()
390  § ---------- persistent settings + game archive (localStorage) ----------
392    fn lsGet()
393    fn lsSet()
394    fn lsDel()
396    fn saveSettings()
401    fn loadSettings()
413    fn loadHistory()
415    fn persistHistory()
420    fn archiveGame()
423    fn serializePlayers()
433    fn serializeGame()
440  § ---- resume: snapshot the game at each turn boundary so a reload can continue ----
441    fn saveResume()
449    fn loadResume()
453    fn clearResume()
457    fn isOwnerP()
458    fn ownerTokenHTML()
459    fn ownerOf()
460    fn ensureOwner()
462    fn applyOwnerName()
482    fn qmarkHTML()
491    fn pickFreeColor()
498    fn dedupeColors()
509    fn dedupeNames()
523    fn initials()
527    fn esc()
532    fn pipFace()
543    fn paintCamo()
566    fn medalSVG()
```

## `features/exp/exp.js`
```
3  § ============================================================ EXPERIMENTAL flow (single column)
7    fn expBeginTurn()
32    fn expStartLog()
33    fn expLogReroll()
34    fn expFirstRoll()
43    fn expHumanFire()
56    fn expCommit()
100    fn expEndTurn()
112    fn expRunAiTurn()
114  § ----- EXACT penalty-aware evaluator (reuses the engine reroll DP, threading the upper subtotal) -----
115    fn expStageUp()
120    fn expCellValue()
127    fn expV0M()
136    fn expV0()
137    fn expExpectRoll()
139  § ----- full per-game analysis (mirrors EV.analyzeTurn/analyzeGame, threading the upper subtotal,
142    fn expSeverityOf()
143    fn expStageOf()
144    fn expAvg()
145    fn analyzeTurnExp()
174    fn expAggregate()
191    fn analyzeGameExp()
212    fn analyzeManualGameExp()
231  § ----- live hints (Щабът съветва), exact-EV backed -----
233    fn expKeepValue()
238    fn expRenderHint()
264    fn expRankCategories()
270    fn expRankKeeps()
278    fn expBotCategory()
286    fn expBotKeep()
295    fn expAiKeeps()
300    fn expAiStep()
321    fn expChooseKeyEV()
334    fn expAiFinish()
345  § ----- experimental rendering -----
353    fn expMiniBoard()
369    fn expRenderBoard()
415    fn showNumPartTip()
431    fn expEndGame()
436    fn archiveExpGame()
```

## `features/game/ai.js`
```
3  § ---------- AI ----------
5    fn aiKeepMask()
12    fn aiCategory()
19    fn runAiTurn()
20    fn aiStep()
43    fn aiFinish()
52  § ---------- speech bubbles ----------
56    fn showOrder()
74    fn showRoast()
88    fn clearRoast()
90  § ---------- combo reminder tooltip + a random (non-permanent) penalty ----------
92    fn shuffled()
93    fn catLabel()
99    fn comboReminder()
122    fn penAnchor()
136    fn showGuide()
145    fn showFxBubble()
157    fn applyPenalty()
211    fn expirePenalty()
219    fn clearAllPenalties()
228    fn rebuildFx()
258    fn syncPenalties()
```

## `features/game/game.js`
```
4  § ===================================================== GAME STATE
13    fn freshTurn()
17    fn startTurnLog()
30    fn netWatching()
35    fn catIndexOf()
36    fn catKeyAt()
44    fn startGame()
69    fn resumeGame()
85    fn maybeOfferResume()
102    fn maybeJoinFromURL()
116    fn maybeOfferNetRejoin()
144    fn beginTurn()
179    fn firstRoll()
193  § ---------- previews ----------
194    fn computePreviews()
206  § ---------- render ----------
212    fn renderAll()
227    fn hintDie()
231    fn keepForTarget()
246    fn renderHint()
290    fn renderHeader()
325    fn pillInk()
330    fn isMyNetPlayer()
331    fn filledCount()
333    fn isMamnik()
340    fn pillHTML()
346    fn renderPills()
360    fn attachTip()
362    fn boardKeys()
366    fn renderBoard()
443  § ---------- manual mode (ОТЧЕТ): tap in all 5 table dice, then pick a combo ----------
447    fn manualDiceArray()
449    fn renderManualDock()
464    fn tapManualDie()
474    fn popUndo()
491    fn renderDice()
535    fn renderFire()
574  § ---------- dice interaction (select-to-reroll) ----------
576    fn toggleSelect()
623    fn syncRollAll()
644    fn activeKeep()
650    fn activeBatch()
655    fn renderKeepThrowTip()
663    fn showKeepThrowTip()
704    fn rerollMask()
708    fn applyReroll()
713    fn humanFire()
730    fn sortDice()
732    fn shakeDice()
738  § ---------- commit / turn flow ----------
739    fn commitScore()
746    fn commitForfeit()
753    fn afterCommit()
796    fn flashTile()
809    fn endTurn()
```

## `features/history/history.js`
```
3  § ============================================================ ARCHIVE / HISTORY
6    fn recIsExp()
10    fn recIsNet()
22    fn recTotal()
27    fn recPlacement()
28    fn recOwnerIdx()
33    fn gameRanking()
40    fn analyzeRec()
46    fn fmtDate()
47    fn fmtTime()
50    fn reconstructPlayers()
59    fn openHistoryGame()
76    fn exitHistoryGame()
90  § ---------- §8 replay viewer (turn-by-turn + roll-by-roll, scrubbable) ----------
95    fn replayGens()
111    fn buildReplayActions()
142    fn rpStateAt()
150    fn replayBoardHTML()
164    fn renderReplay()
206    fn syncSpeedBtn()
212    fn buildFilterMenu()
222    fn setReplayFilter()
231    fn rpLast()
232    fn rpPause()
233    fn rpSchedule()
240    fn rpPlay()
244    fn openReplay()
309    fn dismissTransients()
324    fn closeReplay()
329    fn stdev()
335    fn trendSlope()
343    fn computeOwnerCareer()
414  § ----- §2.7 per-battle career charts (points / skill / luck over recent games) -----
420    fn ownerChartSeries()
428    fn makeOwnerChart()
470    fn buildOwnerCharts()
493    fn wireOwnerCharts()
532    fn ownerOverview()
605    fn careerCatBoard()
636    fn careerAverages()
659    fn ownerScorePercentile()
666    fn setHistTab()
670    fn renderHistory()
697    fn wireClearFilter()
701    fn renderHistoryList()
784    fn playFilterHeaderHTML()
790    fn wirePlayFilter()
797  § ---- swipe gestures on a history row ------------------------------------------------
802    fn attachSwipe()
841    fn pulseFirstRow()
846  § ---------- §ист battle calendar (git-style activity heatmap over ALL rulesets) ----------
850    fn dayStart()
852    fn recRepScore()
858    fn calData()
866    fn calLevel()
867    fn openCalendar()
874    fn renderCalendar()
```

## `features/modals/modals.js`
```
3  § ---------- how to play + in-game menu ----------
4    fn buildHowTo()
22    fn openHowto()
31  § ---------- combo reference sheet (quick-glance scoring table) ----------
42    fn miniDie()
44    fn uiRuleset()
65    fn buildComboSheet()
83    fn openCombos()
85    fn closeCombos()
106    fn abortToStart()
121  § ---------- settings (with a secret developer mode) ----------
122    fn syncHintBtn()
154    fn buildSettingsRows()
182    fn clearSelection()
208    fn activateDev()
212    fn pageShareURL()
213    fn openQrShare()
234    fn el()
246  § ---- dev-mode editor: ALL game strings, edit-on-touch, diff-only copy ----
251    fn bracketRank()
252    fn cloneVal()
254    fn devArea()
262  § ===================== placeholder template editor (chips + variable picker) =====================
284    fn varLabelFor()
286    fn parseTemplate()
295    fn devChip()
301    fn serializeEditable()
316    fn devTemplateEditor()
342    fn devVarPop()
357    fn varPopOutside()
358    fn closeVarPop()
360    fn maxSameCount()
362    fn catMaxFor()
363    fn gameVarCtx()
373    fn fillTemplate()
375    fn buildDevCats()
402    fn devItemText()
406    fn devBadge()
412  § ----- developer string editor (its own modal; in-place expand/collapse editing) -----
419    fn valEqual()
420    fn recomputeStatus()
421    fn devSecLabelHTML()
426    fn tmplPreviewHTML()
432    fn devItemHeadHTML()
439    fn itemsSorted()
440    fn refreshDevHead()
441    fn refreshDevSecBadge()
442    fn addDevItem()
443    fn collapseDevEdit()
444    fn buildDevEditForm()
476    fn toggleDevEdit()
482    fn devApply()
483    fn devCancel()
484    fn devRestore()
485    fn devRemove()
487    fn attachDevSwipe()
512    fn buildDevItem()
520    fn buildDevSection()
530  § ----- dev control sections (toggles / actions) above the string editor -----
531    fn devControlSection()
537    fn devToggleRow()
545    fn greyDevRow()
550    fn renderDevControls()
571    fn renderDevModal()
580    fn devApplyAllBtn()
588    fn openDevModal()
589    fn closeDevModal()
595    fn showDevClosePrompt()
606  § ----- copy: only the changes, with an executable identifier -----
607    fn wordJSON()
608    fn diffVal()
609    fn serializeDevDiff()
622  § ----- live preview of the applyable subset (word pools + roast/shame/combos/bets) -----
623    fn devApplyAll()
633    fn openSettings()
657    fn closeSettings()
665  § ---------- peek (mini scoreboard, like the in-game one) ----------
666    fn miniBoard()
677    fn openPeek()
```

## `features/net/net.js`
```
3  § ===================================================== lobby flavour + net link status
13    fn setFlavour()
20    fn stopFlavour()
24    fn syncNetLink()
37    fn netOnReject()
48    fn netSay()
49    fn localMeta()
54    fn renderNetRoster()
61    fn netCallbacks()
99    fn netOnWait()
105    fn netOnDrop()
113    fn netNotice()
121    fn syncNetDropped()
127    fn netApplySnapshot()
140  § ---------- lobby preparation UI ----------
141    fn netShow()
151    fn settingsBits()
152    fn renderSettSummary()
160    fn netMyEntry()
162    fn enterNetPrep()
176    fn renderNetMeCard()
202    fn setMyReady()
204    fn markEdited()
209    fn touchedField()
211    fn pushMyMeta()
216  § ---------- „ДАЙ ЗОР" lobby cheer: a spammable button that spurs YOUR entry; heat reddens the
222    fn nowMs()
225    fn spurMsgForClicks()
232    fn garbleFor()
240    fn spurBump()
246    fn spurMe()
252    fn onNetSpur()
257    fn startSpurLoop()
258    fn spurTick()
269    fn applySpurVisual()
281    fn resetSpur()
282    fn renderNetPrep()
320    fn selExp()
321    fn netRounds()
323    fn netRuleset()
324    fn newSessionWith()
329    fn wrEphKey()
330    fn wrLoadEph()
331    fn wrSaveEph()
335    fn netActiveSave()
343    fn netActiveLoad()
345    fn netActiveTouch()
349    fn netActiveClear()
360  § ===================================================== WebRTC transport (PeerJS) ==============
379    fn customIce()
383    fn peerOpts()
384    fn genGameCode()
385    fn loadPeerJS()
393  § ---------- WebRTC capture (records every connection event + frame for offline debugging) ----------
395    fn wrTypeName()
396    fn wrlog()
403    fn wrCapReset()
409    fn wrFrame()
414    fn wrPeerErr()
417    fn candType()
418    fn wrAttachIce()
438    fn syncWrCapStatus()
439    fn syncWrCapVis()
444    fn syncIceField()
445    fn iceStatus()
450    fn PeerBus()
559    fn openNetModal()
577    fn rsLabel()
578    fn modeLabel()
580    fn netJoinMismatch()
588    fn renderNetPickInfo()
593    fn netChooseMsg()
607    fn codeScanShow()
620    fn extractGameCode()
629    fn flashCodeGreen()
662  § ---- WebRTC host: mint a code, open the PeerJS link, then run the normal lobby ----
666    fn codeCopyLine()
674    fn wireCodeCopy()
685    fn shareCodeText()
688    fn joinURL()
690    fn parsePastedCode()
701    fn shareGameCode()
707    fn renderCodeQRInto()
717    fn renderCodeQR()
726    fn renderHostCode()
735    fn copyToClipboard()
752    fn copyGameCode()
761    fn flashCopied()
768    fn webrtcHostRestore()
784    fn webrtcHost()
800  § ---- WebRTC client: dial the host's code, then ask to join the lobby ----
831  § ---- connection-loss handling ----
833    fn netHostLost()
840    fn netClientLost()
844  § ===================================================== QR scan / display helpers (shared: WebRTC join-by-scan + invite QR)
845    fn loadScriptOnce()
846    fn loadOpticalLibs()
855  § ---- QR scan / display UI helpers ----
856    fn optStatus()
857    fn optShow()
861    fn optHideView()
862    fn optNextBtn()
864    fn startScan()
884    fn stopScan()
922    fn netDisposeSession()
938    fn netHostGone()
944    fn leaveNet()
946    fn netBackToChoose()
949    fn netAtPicker()
962  § ---- WebRTC capture export (base64 JSON of every logged event) ----
980    fn startNetGame()
1016    fn beginManualEntry()
1024    fn myManualDone()
1025    fn netSetTurn()
1035  § ---------- live spectating: broadcast my actions; watch the active player's actions ----------
1038    fn netSendAct()
1048  § ---- spectating: a calm, QUEUED replay of the watched player's actions (~2s apart) so a fast
1054    fn resetSpecPlay()
1055    fn netSpectate()
1065    fn specPump()
1071    fn playSpecAction()
1089    fn specThrownPositions()
1095    fn specSetDice()
1116    fn previewSelf()
1127    fn returnToCurrent()
1143    fn syncSpecReturn()
1145    fn devMockLobby()
1160    fn devMockLobbyClient()
1175    fn netAiPolicy()
1181    fn runNetAiTurn()
1194    fn netApplyRemote()
1213    fn renderAiTakeover()
1235    fn renderMenuNetCode()
1245    fn renderMenuWrLog()
```

## `features/settings/settings.js`
```
3  § ---- export / import a game (share local history across devices) ----
5    fn exportGame()
12    fn copyExport()
22    fn validateGameJSON()
36    fn importReset()
42    fn openImport()
50    fn importError()
51    fn importSelect()
55    fn showImportChoice()
70    fn showImportOwner()
86    fn showImportMerge()
106    fn importStep()
120    fn doMergePlayer()
129    fn mergePlayerIntoGame()
144    fn fileImportedRecord()
163    fn syncWebrtcUI()
167    fn openHistory()
177    fn showHistory()
191  § ---------- owner name + clear-archive controls (settings) ----------
202    fn skipBtnLabel()
203    fn showOwnerInfo()
236    fn hideColorPop()
239    fn freeSwatches()
246    fn openColorPop()
263    fn showColorPop()
290    fn syncOwnerColor()
291  § ---- ruleset picker (Стандартен / Експериментален) ----
292    fn syncRuleset()
301    fn showRuleBubble()
309    fn syncOwnerGender()
322    fn syncOwnerKeep()
326    fn syncOwnerBatch()
330  § ---- localStorage fullness gauge (next to the clear-archive control) ----
332    fn lsBytes()
337    fn updateStorageInfo()
344  § ---- bulk-clear: pick which saved games to delete (all preselected) ----
346    fn openClearPanel()
352    fn renderClearList()
367    fn syncDeleteBtn()
392    fn resetNet()
```

## `features/setup/setup.js`
```
3  § ===================================================== SETUP
10    fn regenName()
18    fn swapCensorName()
29    fn setGender()
41    fn rarityBubbleEl()
55    fn updateCardBubble()
59    fn addSetupPlayer()
75    fn removeSetupPlayer()
82    fn reclaimOwner()
95    fn attachGenderSwipe()
117    fn renderSetup()
269    fn attachSwipeRemove()
314    fn dragAfter()
325    fn flipReorder()
337    fn armCardDrag()
359    fn beginCardDrag()
367    fn onDragMove()
373    fn endCardDrag()
384    fn buildPlayers()
396    fn startFromSetup()
401  § ---- start-screen selectors: ruleset (без/с минуси) + where (device/network) ----
403    fn syncStartRuleSel()
404    fn syncStartWhereSel()
410    fn showInfoToast()
421    fn setStartRuleset()
426    fn setStartWhere()
436    fn attachSelSwipe()
467    fn startNetFlow()
471  § ---- play button: one surface. Tap the ACTIVE end or the middle = старт; tap the INACTIVE end =
474    fn syncPlayBtn()
475    fn setPlayMode()
476    fn startPlay()
```

## `features/summary/summary.js`
```
3  § ---------- end game + tie-break ----------
5    fn saveCurrentGame()
14    fn endGame()
26    fn startTieBreak()
32    fn renderTie()
58    fn catLabelOf()
63    fn sumExp()
64    fn sumCats()
65    fn sumLabel()
68    fn netComplete()
69    fn isExcluded()
71    fn showGameOver()
120    fn setOverTitle()
139    fn summaryRec()
140    fn setMpResult()
149    fn renderMpResult()
159    fn startRename()
179    fn renameGame()
193  § ---- §1 shared award metadata: points winner, skill leader, luck champ, category board ----
194    fn computeSummaryMeta()
239    fn renderWinHeadline()
261    fn verdictFor()
295    fn renderWinBadges()
320    fn showMedalTip()
331    fn showWinTip()
346    fn renderSummary()
426    fn rgba()
435    fn renderCatBoard()
489    fn progressSeries()
504    fn milestonesFor()
527    fn optimalSeries()
534    fn luckSeries()
543    fn renderProgressChart()
638    fn signed()
640    fn deltaTag()
645  § ---- tappable explainer terms ----------------------------------------------
678    fn xterm()
679    fn slbl()
684    fn stat()
686    fn statRows()
690    fn nSigned()
691    fn nBad()
692    fn nNeut()
694  § ---- keep-pattern metrics (dice games only; manual turns have no rolls/keeps) ----
696    fn throwKeepDist()
705    fn diceKeepDist()
714    fn diceDropDist()
722    fn kddie()
723    fn diceRowHTML()
724    fn throwKeepHTML()
733    fn diceKeepHTML()
744    fn rollGens()
761    fn turnDiceHTML()
792    fn turnQuality()
799    fn thumbSVG()
804    fn qualityIcon()
815    fn turnComment()
855    fn evNum()
865    fn renderReport()
1021    fn decisionLabel()
1030    fn coachLine()
```

## `features/tutorial/tutorial.js`
```
3  § ========================================================== TUTORIAL (scripted dice + coach bubbles)
54    fn tutAnchor()
61    fn tutPlaceStatic()
66    fn tutClearSpot()
67    fn tutShow()
92    fn tutCopyTexts()
104    fn tutAdvance()
105    fn tutGate()
106    fn tutCommitOk()
107    fn tutEvent()
113    fn tutNudge()
114    fn tutReroll()
129    fn tutStart()
145    fn tutEnd()
152    fn confirmDeleteGame()
161    fn deleteGame()
166    fn toggleExcludeGame()
```

