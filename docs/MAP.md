# Repo map (auto-generated — `bash scripts/genmap.sh`; do not hand-edit)

Per-file index of section banners (`// ===== / // -----`) and top-level `function`s,
with line numbers. Grep/jump here instead of reading whole files. See CLAUDE.md for architecture.

## `features/core/boot.js`
```
  (no banners/functions)
```

## `features/core/core.js`
```
33    fn expUpperComplete()
34    fn vstarExp()
61  § ---- analytics (GoatCounter custom events; cookieless, no personal data) ----
67    fn _gcSend()
68    fn track()
82    fn trackGame()
332    fn renderChangelog()
340    fn toggleChangelog()
370    fn fun()
371    fn namePointsOn()
372    fn total()
375    fn gExp()
378    fn gManual()
380    fn setDockUI()
385    fn measureBarFull()
394    fn syncBottomPad()
402  § ---------- persistent settings + game archive (localStorage) ----------
404    fn lsGet()
405    fn lsSet()
406    fn lsDel()
408    fn saveSettings()
413    fn loadSettings()
425    fn loadHistory()
427    fn persistHistory()
432    fn archiveGame()
435    fn serializePlayers()
445    fn serializeGame()
452  § ---- resume: snapshot the game at each turn boundary so a reload can continue ----
453    fn saveResume()
461    fn loadResume()
465    fn clearResume()
469    fn isOwnerP()
470    fn ownerTokenHTML()
471    fn ownerOf()
472    fn ensureOwner()
474    fn applyOwnerName()
494    fn qmarkHTML()
503    fn pickFreeColor()
510    fn dedupeColors()
521    fn dedupeNames()
535    fn initials()
539    fn esc()
544    fn pipFace()
555    fn paintCamo()
578    fn medalSVG()
```

## `features/exp/exp.js`
```
3  § ============================================================ EXPERIMENTAL flow (single column)
7    fn expBeginTurn()
32    fn expStartLog()
33    fn expLogReroll()
34    fn expFirstRoll()
44    fn expHumanFire()
58    fn expCommit()
102    fn expEndTurn()
114    fn expRunAiTurn()
116  § ----- EXACT penalty-aware evaluator (reuses the engine reroll DP, threading the upper subtotal) -----
117    fn expStageUp()
122    fn expCellValue()
129    fn expV0M()
138    fn expV0()
139    fn expExpectRoll()
141  § ----- full per-game analysis (mirrors EV.analyzeTurn/analyzeGame, threading the upper subtotal,
144    fn expSeverityOf()
145    fn expStageOf()
146    fn expAvg()
147    fn analyzeTurnExp()
176    fn expAggregate()
193    fn analyzeGameExp()
214    fn analyzeManualGameExp()
233  § ----- live hints (Щабът съветва), exact-EV backed -----
235    fn expKeepValue()
240    fn expRenderHint()
266    fn expRankCategories()
272    fn expRankKeeps()
280    fn expBotCategory()
288    fn expBotKeep()
297    fn expAiKeeps()
302    fn expAiStep()
323    fn expChooseKeyEV()
336    fn expAiFinish()
347  § ----- experimental rendering -----
355    fn expMiniBoard()
371    fn expRenderBoard()
417    fn showNumPartTip()
433    fn expEndGame()
438    fn archiveExpGame()
```

## `features/game/ai.js`
```
3  § ---------- AI ----------
5    fn aiKeepMask()
12    fn aiCategory()
19    fn runAiTurn()
20    fn aiStep()
43    fn aiFinish()
52  § ---------- speech bubbles ----------
56    fn showOrder()
74    fn showRoast()
88    fn clearRoast()
90  § ---------- combo reminder tooltip + a random (non-permanent) penalty ----------
92    fn shuffled()
93    fn catLabel()
99    fn comboReminder()
122    fn penAnchor()
136    fn showGuide()
145    fn showFxBubble()
157    fn applyPenalty()
211    fn expirePenalty()
219    fn clearAllPenalties()
228    fn rebuildFx()
258    fn syncPenalties()
```

## `features/game/game.js`
```
4  § ===================================================== GAME STATE
13    fn freshTurn()
17    fn startTurnLog()
30    fn netWatching()
35    fn catIndexOf()
36    fn catKeyAt()
44    fn startGame()
69    fn resumeGame()
85    fn maybeOfferResume()
102    fn maybeJoinFromURL()
116    fn maybeOfferNetRejoin()
144    fn beginTurn()
179    fn firstRoll()
193  § ---------- previews ----------
194    fn computePreviews()
206  § ---------- render ----------
212    fn renderAll()
228    fn hintDie()
232    fn keepForTarget()
247    fn renderHint()
291    fn renderHeader()
326    fn pillInk()
331    fn isMyNetPlayer()
332    fn filledCount()
334    fn isMamnik()
341    fn pillHTML()
348    fn renderPills()
370    fn attachTip()
372    fn boardKeys()
376    fn renderBoard()
453  § ---------- manual mode (ОТЧЕТ): tap in all 5 table dice, then pick a combo ----------
457    fn manualDiceArray()
459    fn renderManualDock()
474    fn tapManualDie()
484    fn popUndo()
501    fn renderDice()
545    fn renderFire()
584  § ---------- dice interaction (select-to-reroll) ----------
586    fn toggleSelect()
633    fn syncRollAll()
654    fn activeKeep()
660    fn activeBatch()
665    fn renderKeepThrowTip()
673    fn showKeepThrowTip()
714    fn rerollMask()
718    fn applyReroll()
723    fn humanFire()
740    fn sortDice()
742    fn shakeDice()
748  § ---------- commit / turn flow ----------
749    fn commitScore()
756    fn commitForfeit()
763    fn afterCommit()
806    fn flashTile()
819    fn endTurn()
```

## `features/history/history.js`
```
3  § ============================================================ ARCHIVE / HISTORY
6    fn recIsExp()
10    fn recIsNet()
22    fn recTotal()
27    fn recPlacement()
28    fn recOwnerIdx()
33    fn gameRanking()
40    fn analyzeRec()
46    fn fmtDate()
47    fn fmtTime()
50    fn reconstructPlayers()
59    fn openHistoryGame()
76    fn exitHistoryGame()
90  § ---------- §8 replay viewer (turn-by-turn + roll-by-roll, scrubbable) ----------
95    fn replayGens()
111    fn buildReplayActions()
142    fn rpStateAt()
150    fn replayBoardHTML()
164    fn renderReplay()
206    fn syncSpeedBtn()
212    fn buildFilterMenu()
222    fn setReplayFilter()
231    fn rpLast()
232    fn rpPause()
233    fn rpSchedule()
240    fn rpPlay()
244    fn openReplay()
309    fn dismissTransients()
324    fn closeReplay()
329    fn stdev()
335    fn trendSlope()
343    fn computeOwnerCareer()
414  § ----- §2.7 per-battle career charts (points / skill / luck over recent games) -----
420    fn ownerChartSeries()
428    fn makeOwnerChart()
470    fn buildOwnerCharts()
493    fn wireOwnerCharts()
532    fn ownerOverview()
605    fn careerCatBoard()
636    fn careerAverages()
659    fn ownerScorePercentile()
666    fn setHistTab()
670    fn renderHistory()
697    fn wireClearFilter()
701    fn renderHistoryList()
784    fn playFilterHeaderHTML()
790    fn wirePlayFilter()
797  § ---- swipe gestures on a history row ------------------------------------------------
802    fn attachSwipe()
841    fn pulseFirstRow()
846  § ---------- §ист battle calendar (git-style activity heatmap over ALL rulesets) ----------
850    fn dayStart()
852    fn recRepScore()
858    fn calData()
866    fn calLevel()
867    fn openCalendar()
874    fn renderCalendar()
```

## `features/modals/modals.js`
```
3  § ---------- how to play + in-game menu ----------
4    fn buildHowTo()
22    fn openHowto()
31  § ---------- combo reference sheet (quick-glance scoring table) ----------
42    fn miniDie()
44    fn uiRuleset()
65    fn buildComboSheet()
83    fn openCombos()
85    fn closeCombos()
106    fn abortToStart()
121  § ---------- settings (with a secret developer mode) ----------
122    fn syncHintBtn()
154    fn buildSettingsRows()
182    fn clearSelection()
208    fn activateDev()
212    fn pageShareURL()
213    fn openQrShare()
234    fn el()
246  § ---- dev-mode editor: ALL game strings, edit-on-touch, diff-only copy ----
251    fn bracketRank()
252    fn cloneVal()
254    fn devArea()
262  § ===================== placeholder template editor (chips + variable picker) =====================
284    fn varLabelFor()
286    fn parseTemplate()
295    fn devChip()
301    fn serializeEditable()
316    fn devTemplateEditor()
342    fn devVarPop()
357    fn varPopOutside()
358    fn closeVarPop()
360    fn maxSameCount()
362    fn catMaxFor()
363    fn gameVarCtx()
373    fn fillTemplate()
375    fn buildDevCats()
402    fn devItemText()
406    fn devBadge()
412  § ----- developer string editor (its own modal; in-place expand/collapse editing) -----
419    fn valEqual()
420    fn recomputeStatus()
421    fn devSecLabelHTML()
426    fn tmplPreviewHTML()
432    fn devItemHeadHTML()
439    fn itemsSorted()
440    fn refreshDevHead()
441    fn refreshDevSecBadge()
442    fn addDevItem()
443    fn collapseDevEdit()
444    fn buildDevEditForm()
476    fn toggleDevEdit()
482    fn devApply()
483    fn devCancel()
484    fn devRestore()
485    fn devRemove()
487    fn attachDevSwipe()
512    fn buildDevItem()
520    fn buildDevSection()
530  § ----- dev control sections (toggles / actions) above the string editor -----
531    fn devControlSection()
537    fn devToggleRow()
545    fn greyDevRow()
550    fn renderDevControls()
571    fn renderDevModal()
580    fn devApplyAllBtn()
588    fn openDevModal()
589    fn closeDevModal()
595    fn showDevClosePrompt()
606  § ----- copy: only the changes, with an executable identifier -----
607    fn wordJSON()
608    fn diffVal()
609    fn serializeDevDiff()
622  § ----- live preview of the applyable subset (word pools + roast/shame/combos/bets) -----
623    fn devApplyAll()
633    fn openSettings()
657    fn closeSettings()
665  § ---------- peek (mini scoreboard, like the in-game one) ----------
666    fn miniBoard()
677    fn openPeek()
```

## `features/net/net.js`
```
3  § ===================================================== lobby flavour + net link status
13    fn setFlavour()
20    fn stopFlavour()
24    fn syncNetLink()
47    fn netOnReject()
58    fn netSay()
59    fn localMeta()
64    fn renderNetRoster()
71    fn netCallbacks()
109    fn netOnWait()
115    fn netOnDrop()
123    fn netNotice()
131    fn syncNetDropped()
137    fn netApplySnapshot()
150  § ---------- lobby preparation UI ----------
151    fn netShow()
161    fn settingsBits()
162    fn renderSettSummary()
170    fn netMyEntry()
172    fn enterNetPrep()
186    fn renderNetMeCard()
212    fn setMyReady()
214    fn markEdited()
219    fn touchedField()
221    fn pushMyMeta()
226  § ---------- „ДАЙ ЗОР" lobby cheer: a spammable button that spurs YOUR entry; heat reddens the
232    fn nowMs()
235    fn spurMsgForClicks()
242    fn garbleFor()
250    fn spurBump()
256    fn spurMe()
262    fn onNetSpur()
267    fn startSpurLoop()
268    fn spurTick()
279    fn applySpurVisual()
291    fn resetSpur()
292    fn renderNetPrep()
330    fn selExp()
331    fn netRounds()
333    fn netRuleset()
334    fn newSessionWith()
339    fn wrEphKey()
340    fn wrLoadEph()
341    fn wrSaveEph()
345    fn netActiveSave()
353    fn netActiveLoad()
355    fn netActiveTouch()
359    fn netActiveClear()
370  § ===================================================== WebRTC transport (PeerJS) ==============
389    fn customIce()
393    fn peerOpts()
394    fn genGameCode()
395    fn loadPeerJS()
403  § ---------- WebRTC capture (records every connection event + frame for offline debugging) ----------
405    fn wrTypeName()
406    fn wrlog()
413    fn wrCapReset()
419    fn wrFrame()
424    fn wrPeerErr()
427    fn candType()
428    fn wrAttachIce()
448    fn syncWrCapStatus()
449    fn syncWrCapVis()
454    fn syncIceField()
455    fn iceStatus()
460    fn PeerBus()
569    fn openNetModal()
587    fn rsLabel()
588    fn modeLabel()
590    fn netJoinMismatch()
598    fn renderNetPickInfo()
603    fn netChooseMsg()
617    fn codeScanShow()
630    fn extractGameCode()
639    fn flashCodeGreen()
672  § ---- WebRTC host: mint a code, open the PeerJS link, then run the normal lobby ----
676    fn codeCopyLine()
684    fn wireCodeCopy()
695    fn shareCodeText()
698    fn joinURL()
700    fn parsePastedCode()
711    fn shareGameCode()
717    fn renderCodeQRInto()
727    fn renderCodeQR()
736    fn renderHostCode()
745    fn copyToClipboard()
762    fn copyGameCode()
771    fn flashCopied()
778    fn webrtcHostRestore()
794    fn webrtcHost()
810  § ---- WebRTC client: dial the host's code, then ask to join the lobby ----
841  § ---- connection-loss handling ----
843    fn netHostLost()
850    fn netClientLost()
854  § ===================================================== QR scan / display helpers (shared: WebRTC join-by-scan + invite QR)
855    fn loadScriptOnce()
856    fn loadOpticalLibs()
865  § ---- QR scan / display UI helpers ----
866    fn optStatus()
867    fn optShow()
871    fn optHideView()
872    fn optNextBtn()
874    fn startScan()
894    fn stopScan()
932    fn netDisposeSession()
948    fn netHostGone()
954    fn leaveNet()
956    fn netBackToChoose()
959    fn netAtPicker()
972  § ---- WebRTC capture export (base64 JSON of every logged event) ----
990    fn startNetGame()
1026    fn beginManualEntry()
1034    fn myManualDone()
1035    fn netSetTurn()
1045  § ---------- live spectating: broadcast my actions; watch the active player's actions ----------
1048    fn netSendAct()
1058  § ---- spectating: a calm, QUEUED replay of the watched player's actions (~2s apart) so a fast
1064    fn resetSpecPlay()
1065    fn netSpectate()
1083    fn specPump()
1089    fn playSpecAction()
1107    fn specThrownPositions()
1113    fn specSetDice()
1134    fn previewSelf()
1145    fn returnToCurrent()
1161    fn syncSpecReturn()
1163    fn devMockLobby()
1178    fn devMockLobbyClient()
1193    fn netAiPolicy()
1199    fn runNetAiTurn()
1212    fn netApplyRemote()
1235    fn renderAiTakeover()
1257    fn renderMenuNetCode()
1267    fn renderMenuWrLog()
```

## `features/net/socket-bus.js`
```
31    fn noop()
33  § ---------- url ----------
38    fn normalizeUrl()
58    fn toBytes()
66  § ===================================================== SocketBus
79    fn SocketBus()
96  § ---------- the MP.Session transport contract (send + onReceive) ----------
111  § ---------- lifecycle ----------
```

## `features/settings/settings.js`
```
3  § ---- export / import a game (share local history across devices) ----
5    fn exportGame()
12    fn copyExport()
22    fn validateGameJSON()
36    fn importReset()
42    fn openImport()
50    fn importError()
51    fn importSelect()
55    fn showImportChoice()
70    fn showImportOwner()
86    fn showImportMerge()
106    fn importStep()
120    fn doMergePlayer()
129    fn mergePlayerIntoGame()
144    fn fileImportedRecord()
163    fn syncWebrtcUI()
167    fn openHistory()
177    fn showHistory()
191  § ---------- owner name + clear-archive controls (settings) ----------
202    fn skipBtnLabel()
203    fn showOwnerInfo()
236    fn hideColorPop()
239    fn freeSwatches()
246    fn openColorPop()
263    fn showColorPop()
290    fn syncOwnerColor()
291  § ---- ruleset picker (Стандартен / Експериментален) ----
292    fn syncRuleset()
301    fn showRuleBubble()
309    fn syncOwnerGender()
322    fn syncOwnerKeep()
326    fn syncOwnerBatch()
330  § ---- localStorage fullness gauge (next to the clear-archive control) ----
332    fn lsBytes()
337    fn updateStorageInfo()
344  § ---- bulk-clear: pick which saved games to delete (all preselected) ----
346    fn openClearPanel()
352    fn renderClearList()
367    fn syncDeleteBtn()
392    fn resetNet()
```

## `features/setup/setup.js`
```
3  § ===================================================== SETUP
10    fn regenName()
18    fn swapCensorName()
29    fn setGender()
41    fn rarityBubbleEl()
55    fn updateCardBubble()
59    fn addSetupPlayer()
75    fn removeSetupPlayer()
82    fn reclaimOwner()
95    fn attachGenderSwipe()
117    fn renderSetup()
269    fn attachSwipeRemove()
314    fn dragAfter()
325    fn flipReorder()
337    fn armCardDrag()
359    fn beginCardDrag()
367    fn onDragMove()
373    fn endCardDrag()
384    fn buildPlayers()
396    fn startFromSetup()
401  § ---- start-screen selectors: ruleset (без/с минуси) + where (device/network) ----
403    fn syncStartRuleSel()
404    fn syncStartWhereSel()
410    fn showInfoToast()
421    fn setStartRuleset()
426    fn setStartWhere()
436    fn attachSelSwipe()
467    fn startNetFlow()
471  § ---- play button: one surface. Tap the ACTIVE end or the middle = старт; tap the INACTIVE end =
474    fn syncPlayBtn()
475    fn setPlayMode()
476    fn startPlay()
```

## `features/summary/summary.js`
```
3  § ---------- end game + tie-break ----------
5    fn saveCurrentGame()
14    fn endGame()
26    fn startTieBreak()
32    fn renderTie()
58    fn catLabelOf()
63    fn sumExp()
64    fn sumCats()
65    fn sumLabel()
68    fn netComplete()
69    fn isExcluded()
71    fn showGameOver()
120    fn setOverTitle()
139    fn summaryRec()
140    fn setMpResult()
149    fn renderMpResult()
159    fn startRename()
179    fn renameGame()
193  § ---- §1 shared award metadata: points winner, skill leader, luck champ, category board ----
194    fn computeSummaryMeta()
239    fn renderWinHeadline()
261    fn verdictFor()
295    fn renderWinBadges()
320    fn showMedalTip()
331    fn showWinTip()
346    fn renderSummary()
426    fn rgba()
435    fn renderCatBoard()
489    fn progressSeries()
504    fn milestonesFor()
527    fn optimalSeries()
534    fn luckSeries()
543    fn renderProgressChart()
638    fn signed()
640    fn deltaTag()
645  § ---- tappable explainer terms ----------------------------------------------
678    fn xterm()
679    fn slbl()
684    fn stat()
686    fn statRows()
690    fn nSigned()
691    fn nBad()
692    fn nNeut()
694  § ---- keep-pattern metrics (dice games only; manual turns have no rolls/keeps) ----
696    fn throwKeepDist()
705    fn diceKeepDist()
714    fn diceDropDist()
722    fn kddie()
723    fn diceRowHTML()
724    fn throwKeepHTML()
733    fn diceKeepHTML()
744    fn rollGens()
761    fn turnDiceHTML()
792    fn turnQuality()
799    fn thumbSVG()
804    fn qualityIcon()
815    fn turnComment()
855    fn evNum()
865    fn renderReport()
1021    fn decisionLabel()
1030    fn coachLine()
```

## `features/tutorial/tutorial.js`
```
3  § ========================================================== TUTORIAL (scripted dice + coach bubbles)
54    fn tutAnchor()
61    fn tutPlaceStatic()
66    fn tutClearSpot()
67    fn tutShow()
92    fn tutCopyTexts()
104    fn tutAdvance()
105    fn tutGate()
106    fn tutCommitOk()
107    fn tutEvent()
113    fn tutNudge()
114    fn tutReroll()
129    fn tutStart()
145    fn tutEnd()
152    fn confirmDeleteGame()
161    fn deleteGame()
166    fn toggleExcludeGame()
```

