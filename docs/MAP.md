# Repo map (auto-generated — `bash scripts/genmap.sh`; do not hand-edit)

Per-file index of section banners (`// ===== / // -----`) and top-level `function`s,
with line numbers. Grep/jump here instead of reading whole files. See CLAUDE.md for architecture.

## `features/core/boot.js`
```
  (no banners/functions)
```

## `features/core/core.js`
```
33    fn expUpperComplete()
34    fn vstarExp()
61  § ---- analytics (GoatCounter custom events; cookieless, no personal data) ----
67    fn _gcSend()
68    fn track()
82    fn trackGame()
320    fn renderChangelog()
328    fn toggleChangelog()
358    fn fun()
359    fn namePointsOn()
360    fn total()
363    fn gExp()
366    fn gManual()
368    fn setDockUI()
373    fn measureBarFull()
382    fn syncBottomPad()
390  § ---------- persistent settings + game archive (localStorage) ----------
392    fn lsGet()
393    fn lsSet()
394    fn lsDel()
396    fn saveSettings()
401    fn loadSettings()
413    fn loadHistory()
415    fn persistHistory()
420    fn archiveGame()
423    fn serializePlayers()
433    fn serializeGame()
440  § ---- resume: snapshot the game at each turn boundary so a reload can continue ----
441    fn saveResume()
449    fn loadResume()
453    fn clearResume()
457    fn isOwnerP()
458    fn ownerTokenHTML()
459    fn ownerOf()
460    fn ensureOwner()
462    fn applyOwnerName()
482    fn qmarkHTML()
491    fn pickFreeColor()
498    fn dedupeColors()
509    fn dedupeNames()
523    fn initials()
527    fn esc()
532    fn pipFace()
543    fn paintCamo()
566    fn medalSVG()
```

## `features/exp/exp.js`
```
3  § ============================================================ EXPERIMENTAL flow (single column)
5    fn expStartGame()
24    fn resumeExpGame()
39    fn expBeginTurn()
64    fn expStartLog()
65    fn expLogReroll()
66    fn expFirstRoll()
75    fn expHumanFire()
88    fn expCommit()
132    fn expEndTurn()
144    fn expRunAiTurn()
146  § ----- EXACT penalty-aware evaluator (reuses the engine reroll DP, threading the upper subtotal) -----
147    fn expStageUp()
152    fn expCellValue()
159    fn expV0M()
168    fn expV0()
169    fn expExpectRoll()
171  § ----- full per-game analysis (mirrors EV.analyzeTurn/analyzeGame, threading the upper subtotal,
174    fn expSeverityOf()
175    fn expStageOf()
176    fn expAvg()
177    fn analyzeTurnExp()
206    fn expAggregate()
223    fn analyzeGameExp()
244    fn analyzeManualGameExp()
263  § ----- live hints (Щабът съветва), exact-EV backed -----
265    fn expKeepValue()
270    fn expRenderHint()
296    fn expRankCategories()
302    fn expRankKeeps()
310    fn expBotCategory()
318    fn expBotKeep()
327    fn expAiKeeps()
332    fn expAiStep()
353    fn expChooseKeyEV()
366    fn expAiFinish()
377  § ----- experimental rendering -----
381    fn expRenderHeader()
397    fn expRenderPills()
401    fn expPeek()
411    fn expMiniBoard()
427    fn expRenderBoard()
473    fn showNumPartTip()
489    fn expEndGame()
494    fn archiveExpGame()
```

## `features/game/ai.js`
```
3  § ---------- AI ----------
5    fn aiKeepMask()
12    fn aiCategory()
19    fn runAiTurn()
20    fn aiStep()
43    fn aiFinish()
52  § ---------- speech bubbles ----------
56    fn showOrder()
74    fn showRoast()
88    fn clearRoast()
90  § ---------- combo reminder tooltip + a random (non-permanent) penalty ----------
92    fn shuffled()
93    fn catLabel()
99    fn comboReminder()
122    fn penAnchor()
136    fn showGuide()
145    fn showFxBubble()
157    fn applyPenalty()
211    fn expirePenalty()
219    fn clearAllPenalties()
228    fn rebuildFx()
258    fn syncPenalties()
```

## `features/game/game.js`
```
4  § ===================================================== GAME STATE
13    fn freshTurn()
17    fn startTurnLog()
30    fn netWatching()
35    fn catIndexOf()
36    fn catKeyAt()
42    fn startGame()
68    fn resumeGame()
82    fn maybeOfferResume()
99    fn maybeJoinFromURL()
113    fn maybeOfferNetRejoin()
141    fn beginTurn()
176    fn firstRoll()
190  § ---------- previews ----------
191    fn computePreviews()
203  § ---------- render ----------
209    fn renderAll()
225    fn hintDie()
229    fn keepForTarget()
244    fn renderHint()
284    fn renderHeader()
305    fn pillInk()
310    fn isMyNetPlayer()
311    fn filledCount()
313    fn isMamnik()
320    fn pillHTML()
326    fn renderPills()
340    fn attachTip()
342    fn boardKeys()
346    fn renderBoard()
423  § ---------- manual mode (ОТЧЕТ): tap in all 5 table dice, then pick a combo ----------
427    fn manualDiceArray()
429    fn renderManualDock()
444    fn tapManualDie()
454    fn popUndo()
471    fn renderDice()
515    fn renderFire()
554  § ---------- dice interaction (select-to-reroll) ----------
556    fn toggleSelect()
603    fn syncRollAll()
624    fn activeKeep()
630    fn activeBatch()
635    fn renderKeepThrowTip()
643    fn showKeepThrowTip()
684    fn rerollMask()
688    fn applyReroll()
693    fn humanFire()
710    fn sortDice()
712    fn shakeDice()
718  § ---------- commit / turn flow ----------
719    fn commitScore()
726    fn commitForfeit()
733    fn afterCommit()
776    fn flashTile()
789    fn endTurn()
```

## `features/history/history.js`
```
3  § ============================================================ ARCHIVE / HISTORY
6    fn recIsExp()
10    fn recIsNet()
22    fn recTotal()
27    fn recPlacement()
28    fn recOwnerIdx()
33    fn gameRanking()
40    fn analyzeRec()
46    fn fmtDate()
47    fn fmtTime()
50    fn reconstructPlayers()
59    fn openHistoryGame()
76    fn exitHistoryGame()
90  § ---------- §8 replay viewer (turn-by-turn + roll-by-roll, scrubbable) ----------
95    fn replayGens()
111    fn buildReplayActions()
137    fn rpStateAt()
142    fn replayBoardHTML()
156    fn renderReplay()
198    fn syncSpeedBtn()
204    fn buildFilterMenu()
214    fn setReplayFilter()
223    fn rpLast()
224    fn rpPause()
225    fn rpSchedule()
232    fn rpPlay()
236    fn openReplay()
301    fn dismissTransients()
316    fn closeReplay()
321    fn stdev()
327    fn trendSlope()
335    fn computeOwnerCareer()
406  § ----- §2.7 per-battle career charts (points / skill / luck over recent games) -----
412    fn ownerChartSeries()
420    fn makeOwnerChart()
462    fn buildOwnerCharts()
485    fn wireOwnerCharts()
524    fn ownerOverview()
597    fn careerCatBoard()
628    fn careerAverages()
651    fn ownerScorePercentile()
658    fn setHistTab()
662    fn renderHistory()
689    fn wireClearFilter()
693    fn renderHistoryList()
776    fn playFilterHeaderHTML()
782    fn wirePlayFilter()
789  § ---- swipe gestures on a history row ------------------------------------------------
794    fn attachSwipe()
833    fn pulseFirstRow()
838  § ---------- §ист battle calendar (git-style activity heatmap over ALL rulesets) ----------
842    fn dayStart()
844    fn recRepScore()
850    fn calData()
858    fn calLevel()
859    fn openCalendar()
866    fn renderCalendar()
```

## `features/modals/modals.js`
```
3  § ---------- how to play + in-game menu ----------
4    fn buildHowTo()
22    fn openHowto()
31  § ---------- combo reference sheet (quick-glance scoring table) ----------
42    fn miniDie()
44    fn uiRuleset()
65    fn buildComboSheet()
83    fn openCombos()
85    fn closeCombos()
106    fn abortToStart()
121  § ---------- settings (with a secret developer mode) ----------
122    fn syncHintBtn()
154    fn buildSettingsRows()
182    fn clearSelection()
208    fn activateDev()
212    fn pageShareURL()
213    fn openQrShare()
234    fn el()
246  § ---- dev-mode editor: ALL game strings, edit-on-touch, diff-only copy ----
251    fn bracketRank()
252    fn cloneVal()
254    fn devArea()
262  § ===================== placeholder template editor (chips + variable picker) =====================
284    fn varLabelFor()
286    fn parseTemplate()
295    fn devChip()
301    fn serializeEditable()
316    fn devTemplateEditor()
342    fn devVarPop()
357    fn varPopOutside()
358    fn closeVarPop()
360    fn maxSameCount()
362    fn catMaxFor()
363    fn gameVarCtx()
373    fn fillTemplate()
375    fn buildDevCats()
402    fn devItemText()
406    fn devBadge()
412  § ----- developer string editor (its own modal; in-place expand/collapse editing) -----
419    fn valEqual()
420    fn recomputeStatus()
421    fn devSecLabelHTML()
426    fn tmplPreviewHTML()
432    fn devItemHeadHTML()
439    fn itemsSorted()
440    fn refreshDevHead()
441    fn refreshDevSecBadge()
442    fn addDevItem()
443    fn collapseDevEdit()
444    fn buildDevEditForm()
476    fn toggleDevEdit()
482    fn devApply()
483    fn devCancel()
484    fn devRestore()
485    fn devRemove()
487    fn attachDevSwipe()
512    fn buildDevItem()
520    fn buildDevSection()
530  § ----- dev control sections (toggles / actions) above the string editor -----
531    fn devControlSection()
537    fn devToggleRow()
545    fn greyDevRow()
550    fn renderDevControls()
571    fn renderDevModal()
580    fn devApplyAllBtn()
588    fn openDevModal()
589    fn closeDevModal()
595    fn showDevClosePrompt()
606  § ----- copy: only the changes, with an executable identifier -----
607    fn wordJSON()
608    fn diffVal()
609    fn serializeDevDiff()
622  § ----- live preview of the applyable subset (word pools + roast/shame/combos/bets) -----
623    fn devApplyAll()
633    fn openSettings()
657    fn closeSettings()
665  § ---------- peek (mini scoreboard, like the in-game one) ----------
666    fn miniBoard()
677    fn openPeek()
```

## `features/net/net.js`
```
3  § ===================================================== lobby flavour + net link status
13    fn setFlavour()
20    fn stopFlavour()
24    fn syncNetLink()
37    fn netOnReject()
48    fn netSay()
49    fn localMeta()
54    fn renderNetRoster()
61    fn netCallbacks()
99    fn netOnWait()
105    fn netOnDrop()
113    fn netNotice()
121    fn syncNetDropped()
127    fn netApplySnapshot()
136  § ---------- lobby preparation UI ----------
137    fn netShow()
147    fn settingsBits()
148    fn renderSettSummary()
156    fn netMyEntry()
158    fn enterNetPrep()
172    fn renderNetMeCard()
198    fn setMyReady()
200    fn markEdited()
205    fn touchedField()
207    fn pushMyMeta()
212  § ---------- „ДАЙ ЗОР" lobby cheer: a spammable button that spurs YOUR entry; heat reddens the
218    fn nowMs()
221    fn spurMsgForClicks()
228    fn garbleFor()
236    fn spurBump()
242    fn spurMe()
248    fn onNetSpur()
253    fn startSpurLoop()
254    fn spurTick()
265    fn applySpurVisual()
277    fn resetSpur()
278    fn renderNetPrep()
316    fn selExp()
317    fn netRounds()
319    fn netRuleset()
320    fn newSessionWith()
325    fn wrEphKey()
326    fn wrLoadEph()
327    fn wrSaveEph()
331    fn netActiveSave()
339    fn netActiveLoad()
341    fn netActiveTouch()
345    fn netActiveClear()
356  § ===================================================== WebRTC transport (PeerJS) ==============
375    fn customIce()
379    fn peerOpts()
380    fn genGameCode()
381    fn loadPeerJS()
389  § ---------- WebRTC capture (records every connection event + frame for offline debugging) ----------
391    fn wrTypeName()
392    fn wrlog()
399    fn wrCapReset()
405    fn wrFrame()
410    fn wrPeerErr()
413    fn candType()
414    fn wrAttachIce()
434    fn syncWrCapStatus()
435    fn syncWrCapVis()
440    fn syncIceField()
441    fn iceStatus()
446    fn PeerBus()
555    fn openNetModal()
580    fn syncNetMode()
587    fn rsLabel()
588    fn modeLabel()
590    fn netJoinMismatch()
598    fn renderNetPickInfo()
603    fn netChooseMsg()
618    fn codeScanShow()
631    fn extractGameCode()
640    fn flashCodeGreen()
677  § ---- WebRTC host: mint a code, open the PeerJS link, then run the normal lobby ----
681    fn codeCopyLine()
689    fn wireCodeCopy()
700    fn shareCodeText()
703    fn joinURL()
705    fn parsePastedCode()
716    fn shareGameCode()
722    fn renderCodeQRInto()
732    fn renderCodeQR()
741    fn renderHostCode()
750    fn copyToClipboard()
767    fn copyGameCode()
776    fn flashCopied()
783    fn webrtcHostRestore()
799    fn webrtcHost()
815  § ---- WebRTC client: dial the host's code, then ask to join the lobby ----
847  § ---- connection-loss handling ----
849    fn netHostLost()
856    fn netClientLost()
860  § ===================================================== QR scan / display helpers (shared: WebRTC join-by-scan + invite QR)
861    fn loadScriptOnce()
862    fn loadOpticalLibs()
871  § ---- QR scan / display UI helpers ----
872    fn optStatus()
873    fn optShow()
877    fn optHideView()
878    fn optNextBtn()
880    fn startScan()
900    fn stopScan()
938    fn netDisposeSession()
954    fn netHostGone()
960    fn leaveNet()
962    fn netBackToChoose()
965    fn netAtPicker()
978  § ---- WebRTC capture export (base64 JSON of every logged event) ----
996    fn startNetGame()
1032    fn beginManualEntry()
1040    fn myManualDone()
1041    fn netSetTurn()
1051  § ---------- live spectating: broadcast my actions; watch the active player's actions ----------
1054    fn netSendAct()
1064  § ---- spectating: a calm, QUEUED replay of the watched player's actions (~2s apart) so a fast
1070    fn resetSpecPlay()
1071    fn netSpectate()
1081    fn specPump()
1087    fn playSpecAction()
1104    fn specThrownPositions()
1110    fn specSetDice()
1131    fn previewSelf()
1142    fn returnToCurrent()
1158    fn syncSpecReturn()
1160    fn devMockLobby()
1175    fn devMockLobbyClient()
1191    fn netAiPolicy()
1197    fn runNetAiTurn()
1210    fn netApplyRemote()
1229    fn renderAiTakeover()
1251    fn renderMenuNetCode()
1261    fn renderMenuWrLog()
```

## `features/settings/settings.js`
```
3  § ---- export / import a game (share local history across devices) ----
5    fn exportGame()
12    fn copyExport()
22    fn validateGameJSON()
36    fn importReset()
42    fn openImport()
50    fn importError()
51    fn importSelect()
55    fn showImportChoice()
70    fn showImportOwner()
86    fn showImportMerge()
106    fn importStep()
120    fn doMergePlayer()
129    fn mergePlayerIntoGame()
144    fn fileImportedRecord()
163    fn syncWebrtcUI()
167    fn openHistory()
177    fn showHistory()
191  § ---------- owner name + clear-archive controls (settings) ----------
202    fn skipBtnLabel()
203    fn showOwnerInfo()
236    fn hideColorPop()
239    fn freeSwatches()
246    fn openColorPop()
263    fn showColorPop()
290    fn syncOwnerColor()
291  § ---- ruleset picker (Стандартен / Експериментален) ----
292    fn syncRuleset()
301    fn showRuleBubble()
309    fn syncOwnerGender()
322    fn syncOwnerKeep()
326    fn syncOwnerBatch()
330  § ---- localStorage fullness gauge (next to the clear-archive control) ----
332    fn lsBytes()
337    fn updateStorageInfo()
344  § ---- bulk-clear: pick which saved games to delete (all preselected) ----
346    fn openClearPanel()
352    fn renderClearList()
367    fn syncDeleteBtn()
392    fn resetNet()
```

## `features/setup/setup.js`
```
3  § ===================================================== SETUP
10    fn regenName()
18    fn swapCensorName()
29    fn setGender()
41    fn rarityBubbleEl()
55    fn updateCardBubble()
59    fn addSetupPlayer()
75    fn removeSetupPlayer()
82    fn reclaimOwner()
95    fn attachGenderSwipe()
117    fn renderSetup()
269    fn attachSwipeRemove()
314    fn dragAfter()
325    fn flipReorder()
337    fn armCardDrag()
359    fn beginCardDrag()
367    fn onDragMove()
373    fn endCardDrag()
384    fn buildPlayers()
396    fn startFromSetup()
401  § ---- start-screen selectors: ruleset (без/с минуси) + where (device/network) ----
403    fn syncStartRuleSel()
404    fn syncStartWhereSel()
410    fn showInfoToast()
421    fn setStartRuleset()
426    fn setStartWhere()
436    fn attachSelSwipe()
467    fn startNetFlow()
471  § ---- play button: one surface. Tap the ACTIVE end or the middle = старт; tap the INACTIVE end =
474    fn syncPlayBtn()
475    fn setPlayMode()
476    fn startPlay()
```

## `features/summary/summary.js`
```
3  § ---------- end game + tie-break ----------
5    fn saveCurrentGame()
14    fn endGame()
26    fn startTieBreak()
32    fn renderTie()
58    fn catLabelOf()
63    fn sumExp()
64    fn sumCats()
65    fn sumLabel()
68    fn netComplete()
69    fn isExcluded()
71    fn showGameOver()
120    fn setOverTitle()
139    fn summaryRec()
140    fn setMpResult()
149    fn renderMpResult()
159    fn startRename()
179    fn renameGame()
193  § ---- §1 shared award metadata: points winner, skill leader, luck champ, category board ----
194    fn computeSummaryMeta()
239    fn renderWinHeadline()
261    fn verdictFor()
295    fn renderWinBadges()
320    fn showMedalTip()
331    fn showWinTip()
346    fn renderSummary()
426    fn rgba()
435    fn renderCatBoard()
489    fn progressSeries()
504    fn milestonesFor()
527    fn optimalSeries()
534    fn luckSeries()
543    fn renderProgressChart()
638    fn signed()
640    fn deltaTag()
645  § ---- tappable explainer terms ----------------------------------------------
678    fn xterm()
679    fn slbl()
684    fn stat()
686    fn statRows()
690    fn nSigned()
691    fn nBad()
692    fn nNeut()
694  § ---- keep-pattern metrics (dice games only; manual turns have no rolls/keeps) ----
696    fn throwKeepDist()
705    fn diceKeepDist()
714    fn diceDropDist()
722    fn kddie()
723    fn diceRowHTML()
724    fn throwKeepHTML()
733    fn diceKeepHTML()
744    fn rollGens()
761    fn turnDiceHTML()
792    fn turnQuality()
799    fn thumbSVG()
804    fn qualityIcon()
815    fn turnComment()
855    fn evNum()
865    fn renderReport()
1021    fn decisionLabel()
1030    fn coachLine()
```

## `features/tutorial/tutorial.js`
```
3  § ========================================================== TUTORIAL (scripted dice + coach bubbles)
54    fn tutAnchor()
61    fn tutPlaceStatic()
66    fn tutClearSpot()
67    fn tutShow()
92    fn tutCopyTexts()
104    fn tutAdvance()
105    fn tutGate()
106    fn tutCommitOk()
107    fn tutEvent()
113    fn tutNudge()
114    fn tutReroll()
129    fn tutStart()
145    fn tutEnd()
152    fn confirmDeleteGame()
161    fn deleteGame()
166    fn toggleExcludeGame()
```

